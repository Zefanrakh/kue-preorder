# Supply-chain hardening (README "Dependency dan supply chain").
# Never run package lifecycle scripts (preinstall/install/postinstall): that is
# how npm worms run on install. None of our packages needs them; an explicit
# `npm run <script>` still works.
ignore-scripts=true
# Record exact versions when a dependency is added by hand.
save-exact=true

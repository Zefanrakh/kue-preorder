// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: kuepreorder/orders/v1/checkout.proto

package ordersv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/Zefanrakh/kue-preorder/api/gen/go/kuepreorder/orders/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// CheckoutServiceName is the fully-qualified name of the CheckoutService service.
	CheckoutServiceName = "kuepreorder.orders.v1.CheckoutService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// CheckoutServiceQuoteOrderProcedure is the fully-qualified name of the CheckoutService's
	// QuoteOrder RPC.
	CheckoutServiceQuoteOrderProcedure = "/kuepreorder.orders.v1.CheckoutService/QuoteOrder"
)

// CheckoutServiceClient is a client for the kuepreorder.orders.v1.CheckoutService service.
type CheckoutServiceClient interface {
	// QuoteOrder prices a cart and checks a pickup time without saving
	// anything: what the customer sees before paying. Open to everyone, signed
	// in or not; rate limited per client address (ResourceExhausted). A cart
	// that cannot be ordered is InvalidArgument with FieldErrors on "items" or
	// "pickup_at"; a pickup time that does not work is a rejection in the
	// response, with the nearest time that does.
	QuoteOrder(context.Context, *connect.Request[v1.QuoteOrderRequest]) (*connect.Response[v1.QuoteOrderResponse], error)
}

// NewCheckoutServiceClient constructs a client for the kuepreorder.orders.v1.CheckoutService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewCheckoutServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) CheckoutServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	checkoutServiceMethods := v1.File_kuepreorder_orders_v1_checkout_proto.Services().ByName("CheckoutService").Methods()
	return &checkoutServiceClient{
		quoteOrder: connect.NewClient[v1.QuoteOrderRequest, v1.QuoteOrderResponse](
			httpClient,
			baseURL+CheckoutServiceQuoteOrderProcedure,
			connect.WithSchema(checkoutServiceMethods.ByName("QuoteOrder")),
			connect.WithIdempotency(connect.IdempotencyNoSideEffects),
			connect.WithClientOptions(opts...),
		),
	}
}

// checkoutServiceClient implements CheckoutServiceClient.
type checkoutServiceClient struct {
	quoteOrder *connect.Client[v1.QuoteOrderRequest, v1.QuoteOrderResponse]
}

// QuoteOrder calls kuepreorder.orders.v1.CheckoutService.QuoteOrder.
func (c *checkoutServiceClient) QuoteOrder(ctx context.Context, req *connect.Request[v1.QuoteOrderRequest]) (*connect.Response[v1.QuoteOrderResponse], error) {
	return c.quoteOrder.CallUnary(ctx, req)
}

// CheckoutServiceHandler is an implementation of the kuepreorder.orders.v1.CheckoutService service.
type CheckoutServiceHandler interface {
	// QuoteOrder prices a cart and checks a pickup time without saving
	// anything: what the customer sees before paying. Open to everyone, signed
	// in or not; rate limited per client address (ResourceExhausted). A cart
	// that cannot be ordered is InvalidArgument with FieldErrors on "items" or
	// "pickup_at"; a pickup time that does not work is a rejection in the
	// response, with the nearest time that does.
	QuoteOrder(context.Context, *connect.Request[v1.QuoteOrderRequest]) (*connect.Response[v1.QuoteOrderResponse], error)
}

// NewCheckoutServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewCheckoutServiceHandler(svc CheckoutServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	checkoutServiceMethods := v1.File_kuepreorder_orders_v1_checkout_proto.Services().ByName("CheckoutService").Methods()
	checkoutServiceQuoteOrderHandler := connect.NewUnaryHandler(
		CheckoutServiceQuoteOrderProcedure,
		svc.QuoteOrder,
		connect.WithSchema(checkoutServiceMethods.ByName("QuoteOrder")),
		connect.WithIdempotency(connect.IdempotencyNoSideEffects),
		connect.WithHandlerOptions(opts...),
	)
	return "/kuepreorder.orders.v1.CheckoutService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case CheckoutServiceQuoteOrderProcedure:
			checkoutServiceQuoteOrderHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedCheckoutServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedCheckoutServiceHandler struct{}

func (UnimplementedCheckoutServiceHandler) QuoteOrder(context.Context, *connect.Request[v1.QuoteOrderRequest]) (*connect.Response[v1.QuoteOrderResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kuepreorder.orders.v1.CheckoutService.QuoteOrder is not implemented"))
}
